import { Button } from "antd";
import { styled } from "styled-components";

export const StickyButton = styled.div`
    position: fixed;
    top: 6rem;
    right: 0.5rem;
    z-index: 1;

    margin-right: 0.5rem;
`;