import { Button } from "antd";
import { styled } from "styled-components";

export const StickyButton = styled.div`
    position: fixed;
    top: 4.800rem;
    right: 0;
    z-index: 1;

    margin-right: 0.5rem;
`;